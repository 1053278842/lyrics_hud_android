<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/toggle_button_bg"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginStart="30dp"
    android:layout_marginEnd="30dp"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?android:attr/selectableItemBackground"
    app:cardBackgroundColor="@color/gray"
    app:cardCornerRadius="8dp"
    app:cardElevation="4dp">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingStart="18dp"
        android:paddingTop="20dp"
        android:paddingEnd="18dp"
        android:paddingBottom="20dp">

        <ImageView
            android:id="@+id/toggle_button_img"
            android:layout_width="25dp"
            android:layout_height="50dp"
            android:contentDescription="@string/button_status_stopped"
            android:src="@drawable/ic_clash_stopped" />

        <TextView
            android:id="@+id/toggle_button_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="20dp"
            android:layout_toEndOf="@id/toggle_button_img"
            android:text="@string/button_status_stopped"
            android:textAppearance="@style/TextAppearance.AppCompat.Medium"
            android:textColor="@android:color/white" />

        <TextView
            android:id="@+id/toggle_button_summary"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/toggle_button_title"
            android:layout_alignStart="@id/toggle_button_title"
            android:layout_marginTop="5dp"
            android:text="@string/button_status_click_to_start"
            android:textAppearance="@style/TextAppearance.AppCompat.Small"
            android:textColor="@color/lightGray" />
    </RelativeLayout>
</com.google.android.material.card.MaterialCardView>